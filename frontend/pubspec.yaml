name: frontend
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  
  # HTTP
  dio: ^5.11.0
  
  # Almacenamiento
  shared_preferences: ^2.5.5
  flutter_secure_storage: ^10.3.1
  
  # Estado
  provider: ^6.1.5+1
  
  # JWT
  jwt_decoder: ^2.0.1
  
  # PERSISTENCIA LOCAL CON SQFLITE
  sqflite: ^2.4.0
  path_provider: ^2.1.4
  path: ^1.9.0
  intl: ^0.19.0

  # Conectividad para sincronización
  connectivity_plus: ^6.1.3

  # 🔥 NUEVO: Serialización generada (Semana 13)
  json_annotation: ^4.12.0
  sqflite_common_ffi_web: ^1.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # 🔥 NUEVO: Generación de código para serialización (Semana 13)
  build_runner: ^2.4.12
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true